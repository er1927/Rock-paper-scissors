switch alert into innerHTML on div .resut


puedo: en cada event listener para cada boton, poner el mouse event (click) y despues directamente la function donde el usuario eligio efectivamente su opcion:

rock.addEventListener ("click", () => playerSelection("rock"));

---

const rockBtn = document.getElementById('rockBtn')

rockBtn.addEventListener('click', () => console.log("you clicked on rock"))

---

/* tres botones en pantalla cada un con un valor distinto;
necesito el HTML y la variable que los represente en js?

el usuario clickea un boton

ese boton dispara un eventlistener 

el event listener a) inputea el valor del boton clickeado

y b)gatilla la funcion para jugar una ronda

la funcion decide al azar el valor de la eleccion de la computadora

y lo coteja con el boton que fue clickeado por el usuario

ahi corre la if/else que establece que paper > rock > scissors > paper

y alerta sobre el resultado, quien es el ganador */



















---
<button type="button" id="rock">ROCK</button>
<button type="button" id="paper">PAPER</button>


const rockButton = document.getElementById("rock");       
const paperButton = document.getElementById("paper");


rockButton.addEventListener("click", () => console.log("you clicked on rock button")); // pass       
paperButton.addEventlistener("click", () => console.log("you clicked on paper button")); //fail



---

como defino playerSelection en base a los botones

deberia ser de scope global, pero quizas no const, sino let

quizas necesito una funcion con playerselection como parametro? para especificar los valores de piedra papel y tijera?










